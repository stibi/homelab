# Ansible
.ansible/
*.retry
.vault_pass
vault_pass.txt
inventory/host_vars/*/vault.yml

# Local overrides, not for the repo
inventory/local.yml
ansible.local.cfg

# Claude Code machine-local permission grants (settings.json is shareable).
# The .tmp.* glob catches the atomic-write temp files it leaves mid-update.
.claude/settings.local.json
.claude/*.tmp.*

# Editors / OS
.DS_Store
*.swp
*~
.idea/
.vscode/
